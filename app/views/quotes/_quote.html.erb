<div class="quote p-2">
  <%= link_to quote.name, quote_path(quote) %>
  <div class="quote__actions pt-2">
    <%= button_to "Delete", quote_path(quote), method: :delete, class: "btn btn-danger btn-sm" %>
    <%= link_to "Edit", edit_quote_path(quote), class: "btn btn-secondary btn-sm mt-1" %>
  </div>
</div>